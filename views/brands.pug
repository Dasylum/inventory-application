extends layout

block content
    h1= 'Brands'
    each brand in brands    
        a(href=`/cars/${brand._id}`)
            div
                h2 #{brand.name}
                p #{brand.description}
    a(href="/addbrand") Add new manufacturer        